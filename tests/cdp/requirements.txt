# Pinned for reproducibility — Step 10 of chromeless-build.sh launches a
# python:3.11-slim Job that pip-installs from this file. Bumps go
# through a normal review cycle so a transient PyPI yank can't
# silently break promotion.
websockets==12.0
requests==2.32.3
pytest==8.3.3
pytest-asyncio==0.24.0
